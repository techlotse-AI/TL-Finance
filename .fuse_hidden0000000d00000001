# TL-Project — TL-Finance

> **Project tracking file.** This is *not* a release or version file — `package.json`,
> `CHANGELOG.md` and git tags remain authoritative for what has shipped. This file tracks
> the **work**: what's next, when it's due, and a running status log.
>
> The daily TL briefing reads **Next Task** and **Due Date** from this file. It reads
> *Released Version* and *Last Commit* automatically from git/version files. Keep the two
> fields below current as priorities change — everything else is context.

| Field | Value |
|---|---|
| Product | Docker-first, privacy-focused household finance app (Budget / Analyze / Optimize) |
| Repo | `D:\LocalCode\TL-Finance` |
| Released version | v0.8.2 (authoritative: git tag `v0.8.2`) — ⚠️ `package.json` still reads `0.8.1`, bump pending |
| Deployment | Production-ready — versioned, vulnerability-gated Docker Hub releases; public-auth, rate limiting, scheduled backups, deployment runbooks |
| Tracking owner | Ruan |

## Next task
**Start Phase D (v0.9.0 "Resilience & goals") with D3 — Debt payoff calculator** (`src/lib/optimize/debt.ts`). Self-contained, golden-tested, no schema migration — shippable in a day per the 2026-06-20 daily status.

## Due date
2026-06-27  _(proposed — adjust as needed)_

## Outstanding / backlog
- Phase D remainder: D1 income-protection-aware emergency fund, D2 financial goals / sinking funds, D4 net-worth statement.
- Deferred **TOTP 2FA** from Phase A.
- Backlog: FX exposure report, inflation/real-terms toggle, tax-pack export, login alerts, passkeys, ZA country profile.
- `eslint .` was inconclusive in the sandbox (timeout) — rerun locally to confirm clean.

## Status log
- 2026-06-20 — Tracking file created. v0.8.1, working tree clean on `main`; `tsc` passes, 128 tests pass — releasable green state. Pha- 2026-06-22 — Briefing run. Released v0.8.2 (git tag); last commit `1278f55` (2026-06-20, "v0.8.2 new release CI"). `package.json` bump to 0.8.2 still pending. Phase D not yet started — next task unchanged.
 `MIGRATION.md` and `docs/daily/` in working tree. Phase D not yet started.
