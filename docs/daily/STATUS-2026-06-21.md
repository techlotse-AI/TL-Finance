# TL-Finance — Daily Status & Suggestions · 2026-06-21

_Automated daily status run. No code or feature was changed; this is a report. Where the live repo and the tracking file disagree, the repo (git + tests) is treated as authoritative._

## TL;DR

The repo is in a **clean, green state and is materially ahead of `TL-Project.MD`**. The tracking file is stale: it lists v0.8.1 / 128 tests / "Phase D not started", but `main` is at **v0.8.2 / 146 tests passing** and **two Phase D items (D1 emergency fund, D3 debt payoff) are already shipped and fully wired** (lib + API + UI). The most valuable day-sized work now is (a) correcting the tracker, (b) the low-risk **`MIGRATION.md` → 1.0.0-alpha.1** file additions, and (c) the next real feature, **D4 net-worth statement**.

## Verified current state

| Check | Result |
|---|---|
| Branch | `main`, last commit `1278f55` v0.8.2 new release CI (2026-06-20) |
| Released version | **v0.8.2** (tracker still says v0.8.1) |
| Typecheck (`tsc --noEmit`) | **Pass** (exit 0) |
| Tests (`vitest run`) | **146 passed, 5 skipped** (3 skipped files are DB-backed integration tests) |
| Phase D — D1 emergency fund | **Shipped** — `lib/optimize/emergency-fund.ts` + API route + `optimize-workspace.tsx` |
| Phase D — D3 debt payoff | **Shipped** — `lib/optimize/debt.ts` + `api/optimize/debt/route.ts` + UI |
| Phase D — D2 goals / sinking funds | **Not implemented** |
| Phase D — D4 net-worth statement | **Not implemented** |

So the tracker's "Next task = D3 debt payoff" is already done. Phase D is roughly half complete.

## Migration to TL standard (`MIGRATION.md`) — gap check

| Item | Status |
|---|---|
| `.github/dependabot.yml` | ✅ present |
| Trivy CRITICAL gate | ⚠️ only in `ci.yml`; `release.yml` does not exist |
| `VERSION` file (`1.0.0-alpha.1`) | ❌ missing |
| Top-level `CHANGELOG.md` | ❌ still at `docs/release/CHANGELOG.md` |
| `LICENSE.md` | ❌ missing |
| `STYLING.md` | ❌ missing |
| `CLAUDE.md` pointer → `AGENTS.md` | ❌ missing |
| `release.yml` (alpha channel, multi-arch, SBOM) | ❌ missing |
| `docs/` consolidated to standard set | ❌ not done |

Note: `check-release-version.mjs` currently asserts `package.json` == `package-lock.json`. The migration wants `VERSION` as single source of truth, and the alpha plan uses `vX.Y.Z-alpha.N` tags — which the script's `^\d+\.\d+\.\d+$` regex would reject. Reconcile these before flipping to the alpha channel.

## Suggested work for today — pick one track

Ordered by value-for-risk. All are self-contained and testable, matching the project's "accuracy paramount, fully tested" rule.

**Track 1 — Tracker hygiene (≈15 min, do this regardless).** Update `TL-Project.MD`: version v0.8.2, 146 tests, mark D1 + D3 shipped, set Next Task to D4 (or D2), append a status-log line. Keeps the daily briefing trustworthy.

**Track 2 — `1.0.0-alpha.1` migration quick wins (½ day, zero schema risk).** Pure file additions, no runtime behavior change: add `VERSION`, move `CHANGELOG.md` to top-level with a `[Unreleased]` section, add `LICENSE.md` (Techlotse Source-Available), `STYLING.md` from the locked UI spec in `AGENTS.md`, and a `CLAUDE.md` one-line pointer to `AGENTS.md`. Highest leverage toward a "sellable" public alpha. Defer `release.yml` + the version-script change to a focused follow-up since it touches CI and the version-source-of-truth contract.

**Track 3 — Feature: D4 net-worth statement (1 day).** Cleanest remaining Phase D calc. Net worth = positive account/holding balances − debt balances at a reporting date, in one reporting currency. Holdings (`lib/optimize/holdings.ts`) and debt (`lib/optimize/debt.ts`) already exist as inputs, so this is largely an aggregation + reconciliation module: `lib/optimize/net-worth.ts`, golden tests, `api/optimize/net-worth/route.ts`, a workspace panel. Confirm first whether manual cash/property balances need a new (additive) Prisma table — if so, that pushes scope past one day and Track 2 is the safer same-day pick.

**Track 4 — D2 financial goals / sinking funds.** Higher value to end users but almost certainly needs a new schema table (goal, target amount, target date, funding source) plus migration, API, UI and tests — more than a single clean day.

## Recommendation

Do **Track 1** now (trivial, unblocks accurate reporting) and commit **Track 2** as today's shippable change — it advances the public-alpha goal with no schema or runtime risk, which best fits the Swiss-first "polished, sellable" objective. Hold D4 for the next focused session once the manual-balance question is settled.
