# TL-Finance — Daily Status & Suggestions · 2026-06-23

_Automated daily status run. No code or feature was changed; this is a report. Where the live repo and the tracking file disagree, the repo (git + tests) is authoritative._

## TL;DR

The repo is **still clean and green**, unchanged in substance since the 2026-06-21 and 2026-06-22 reports: `main` at **v0.8.2, 146 tests passing (5 skipped), `tsc` clean (exit 0)**. **No commits in the last day** — yesterday's recommended same-day work (Track 1 tracker/migration hygiene, and Track 2 D4-a net-worth) was **not picked up**, and the working tree still carries the same uncommitted items. This is now the **third consecutive day** the trivial hygiene quick-wins have slipped. Recommendation is unchanged but more urgent: do the 30-minute carry-over first, then start D4-a.

## Verified current state (this run)

| Check | Result |
|---|---|
| Branch / last commit | `main`, `1278f55` "v0.8.2 new release CI" (2026-06-20) — **no new commits since 06-20** |
| Released version | **v0.8.2** (git tag) — tracker & `package.json` still read `0.8.1` |
| Typecheck (`tsc --noEmit`) | **Pass** (exit 0) |
| Tests (`vitest run`) | **146 passed, 5 skipped** (3 skipped files are DB-backed integration tests), 18.2s |
| Working tree | Dirty: ` M TL-Project.MD`, `?? MIGRATION.md`, `?? docs/daily/` (same as 06-21/06-22) |
| Phase D — D1 emergency fund | **Shipped** (`lib/optimize/emergency-fund.ts` + API + UI) |
| Phase D — D3 debt payoff | **Shipped** (`lib/optimize/debt.ts` + API + UI) |
| Phase D — D2 goals / sinking funds | **Not implemented** (confirmed: no file in `lib/optimize/`) |
| Phase D — D4 net-worth statement | **Not implemented** (confirmed: no `net-worth.ts`, no `api/optimize/net-worth`) |
| Root files `VERSION`/`CHANGELOG.md`/`LICENSE.md`/`STYLING.md`/`CLAUDE.md` | **All still absent** |

## Carry-over (now slipped 2–3 days)

1. **Tracker hygiene** — `TL-Project.MD` "Next task" still points at **D3 debt payoff, which is already shipped**. The daily briefing reads Next Task / Due Date from this file, so the automation is pointed at completed work. `package.json` (`0.8.1`) and the tracker still lag the `v0.8.2` git tag.
2. **`1.0.0-alpha.1` migration quick wins** — `VERSION`, top-level `CHANGELOG.md`, `LICENSE.md`, `STYLING.md`, and a `CLAUDE.md` → `AGENTS.md` pointer are still missing at repo root.

## Suggested work for today — pick one track

Ordered by value-for-risk. All self-contained and testable, matching the project's "accuracy paramount, fully tested" rule.

**Track 1 — Clear the carry-over (≈30 min, do this regardless).**
Fix the tracker so the briefing is trustworthy, and add the zero-risk alpha files:
- `TL-Project.MD`: set Released version → v0.8.2; set Next task → **D4-a net-worth (derived)**; refresh the test count.
- Add `VERSION` = `1.0.0-alpha.1`, top-level `CHANGELOG.md` with an `[Unreleased]` section, `LICENSE.md`, `STYLING.md` (from the locked UI spec in `AGENTS.md`), and `CLAUDE.md` → `AGENTS.md` pointer.
- ⚠️ **Do not flip the release channel yet.** `scripts/check-release-version.mjs` uses `^\d+\.\d+\.\d+$`, which rejects `-alpha.N` tags, and asserts `package.json` == `package-lock.json`. Adding the `VERSION` file is safe; changing the version contract is a separate follow-up.

**Track 2 — Feature: D4-a net-worth, derived (1 day, no migration).** _De-risked in the 06-22 report._
Ship `lib/optimize/net-worth.ts` + golden tests + `api/optimize/net-worth/route.ts` + a read-only workspace panel. Aggregate already-modelled sources into one reporting currency via `ExchangeRate`: holdings market value (`holdings.ts`) + pension vehicles (`pensions.ts`) + derived account balances − credit-card/debt balances (`debt.ts`). **Label it "financial net worth (modelled accounts only)"** since `Account` has no balance field and there is no manual-asset model yet — property/vehicles are explicitly out of scope here.

**Track 3 — D2 goals / sinking funds, or D4-b manual assets.** Higher end-user value but both need a new Prisma table + migration + API + UI + tests — more than one clean day. Hold for a focused session.

## Recommendation

**Do Track 1 today** — it is now overdue, trivial, zero-runtime-risk, and the tracker is actively misleading the daily briefing. **If time remains, start Track 2 (D4-a):** it is the cleanest remaining Phase D calc, needs no migration, and directly serves the Swiss-first "make finances legible" goal. Defer D2 and D4-b (manual assets) to dedicated sessions because of the schema changes.

## Backlog (unchanged)

Deferred TOTP 2FA (Phase A); FX exposure report; inflation/real-terms toggle; tax-pack export; login alerts; passkeys; ZA country profile. `eslint .` remains unverified in-sandbox (timeout) — run locally to confirm clean.
