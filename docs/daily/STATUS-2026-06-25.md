# TL-Finance — Daily Status — 2026-06-25

_Report-only briefing. No feature code was changed by this run._

## Headline

The biggest change since the last briefing is **uncommitted**: a complete
**D4 net-worth statement** feature now sits in the working tree, along with a
`package.json` bump to **0.8.3** and updated CHANGELOG/ROADMAP/USER_GUIDE/API
docs. None of it is committed or tagged yet — git still reports v0.8.2 as the
authoritative release. **The single highest-value action today is to commit and
tag this work before it can be lost.**

## Repo state

| Signal | Value |
|---|---|
| Last commit | `1278f55` — "v0.8.2 new release CI" (2026-06-20). **Unchanged** — no new commits. |
| Latest git tag | `v0.8.2` (authoritative shipped version) |
| `package.json` | **0.8.3** (bumped in working tree; was 0.8.1 — the long-pending bump is now done, but only in tree) |
| `tsc --noEmit` | ✅ Clean |
| `vitest run` | ✅ **151 passed / 5 skipped** (was 146/5 — +5 from net-worth golden tests) |
| Working tree | ⚠️ **Dirty** — 10 tracked files modified (+195/-8), plus untracked feature source, API route, test, `MIGRATION.md`, and `docs/daily/` |

## What's new in the working tree (uncommitted)

**D4 net-worth statement (Optimize) — appears complete and tested:**
- `src/lib/optimize/net-worth.ts` — pure calculator. Aggregates account-derived
  cash, holdings at market value, and pension balances as assets, minus debts and
  caller-supplied manual liabilities, into a reporting-currency total with
  per-line / per-category reconciliation and missing-rate warnings. Persists
  nothing; never touches Budget.
- `src/lib/optimize/net-worth.test.ts` — 5 golden tests (single- and
  multi-currency, liability classification, missing-rate handling).
- `src/app/api/optimize/net-worth/route.ts` — authenticated POST endpoint.
- `queries.ts` / `schemas.ts` extended; new **Net worth tab** in
  `optimize-workspace.tsx`.
- Docs updated: CHANGELOG (v0.8.3 entry), ROADMAP (Phase D now D1+D3+D4),
  USER_GUIDE, API reference.

This is consistent with the standing plan: D4-a is the no-migration slice of the
net-worth statement (D4-b — persisted manual assets/liabilities — still needs a
schema migration and remains deferred).

**New strategic doc — `MIGRATION.md` (untracked):** describes aligning the repo
to a shared "TL standard," with a **version target of `1.0.0-alpha.1`** and an
**alpha release channel** (`:vX.Y.Z-alpha.N` + `:alpha`, not `:latest`). This
introduces a versioning decision (see risks).

## Tracker / hygiene drift

- ⚠️ **`TL-Project.MD` "Next task" is stale.** It still reads *"Start Phase D
  with D3 — Debt payoff calculator."* D3 already shipped, and D4 is now done in
  the tree. It should point to the **last open Phase D item: D2 — financial
  goals / sinking funds** (needs a schema migration).
- Several long-standing stale flags are now resolved *in tree* but only become
  real once committed: `package.json` → 0.8.3, CHANGELOG/ROADMAP current.
- Still absent at repo root: `VERSION`, `LICENSE`, `CLAUDE.md`, `STYLING.md`.
  (Note: the changelog lives at `docs/release/CHANGELOG.md`, not root — by design.)

## Risks

1. **Loss risk (high).** A full release of working code + docs is uncommitted.
   An accidental `git checkout`/reset or environment wipe loses it.
2. **Version conflict.** `package.json` now says 0.8.3, but `MIGRATION.md` targets
   `1.0.0-alpha.1` on an alpha channel. These need reconciling before the next
   tag so the release line doesn't fork.

## Suggested work for today (pick one)

- **Hygiene first (~30 min):** commit the D4 work and tag **v0.8.3**; update
  `TL-Project.MD` Next task → D2; decide whether the next tag follows 0.8.x or
  jumps to the `1.0.0-alpha.1` line from `MIGRATION.md`.
- **Next feature, no migration (~1 day): FX currency-exposure report (Optimize).**
  Reuse the net-worth rate-map + aggregation pattern to show what share of net
  worth sits in non-reporting currencies (EUR/USD exposure for a CHF household —
  directly Swiss-relevant). Self-contained, golden-testable, no schema change.
- **D2 financial goals / sinking funds:** the last open Phase D item, but it
  requires a Prisma migration — larger than a single clean day.

_Recommendation: do the hygiene commit/tag first (it de-risks a release already
sitting on disk), then start the FX exposure report as the day's feature._
