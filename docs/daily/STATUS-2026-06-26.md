# TL-Finance — Daily Status — 2026-06-26

_Report-only briefing. No feature code was changed in this run._

## Headline

The six-day commit freeze is over. Last briefing (2026-06-25) found everything
stuck at `1278f55` / tag `v0.8.2` with a large **uncommitted** working tree. That
backlog has now been committed and shipped in four releases: **v0.8.3 → v0.8.6**.
Working tree is clean and `package.json` (`0.8.6`) matches tag `v0.8.6`. The
version-vs-tag drift flagged on the last five briefings is **resolved**.

## Repo facts (verified)

| Field | Value |
|---|---|
| Git HEAD | `6c41398` — "Update card.tsx" — 2026-06-26 05:03 +0200 |
| Released version | **v0.8.6** (git tag `v0.8.6`; `package.json` = `0.8.6` ✅ aligned) |
| Tags present | v0.5.0, v0.8.0–v0.8.6 |
| Working tree | Clean (only `.fuse_hidden…` — a sandbox mount artifact, not a real change) |
| Golden tests | **59/59 pass** across `src/lib/optimize` (10 files) |
| Test files in repo | 46 |

### What shipped since v0.8.2

- **v0.8.3** — CI/release split into a `verify` gate + single `publish` job
  (tag-driven, Trivy-gated); **Phase D1**: income-protection-aware emergency fund
  (country-agnostic model + Swiss ALV preset), golden-tested.
- **v0.8.4** — **Phase C**: Pillar 2 (BVG) provider projection override; in-place
  budget item/category edit + guarded delete; app-shell logout. Additive
  migration `20260624000000_v0_8_4_pension_projection_override`.
- **v0.8.5** — sign-in no longer forces existing members to onboarding (active
  household self-heals); admin household-membership manager; admin password reset
  (single + bulk).
- **v0.8.6** — audit log filtering + pagination (DB-side, URL state); delete
  planned account transfers; dependency bumps; Docker/CI Node 24→26.

## Phase D (v0.9.0 "Resilience & goals") — progress

| Item | Status |
|---|---|
| D1 income-protection-aware emergency fund | ✅ shipped (v0.8.3) |
| D3 debt payoff calculator (`debt.ts`) | ✅ shipped (pre-v0.8.3) |
| D4 net-worth statement (`net-worth.ts`, `/api/optimize/net-worth`) | ✅ shipped |
| **D2 financial goals / sinking funds** | ❌ **not built** — no `goals.ts`, no `FinancialGoal` model |

D2 is the only remaining Phase D feature. It needs a new `FinancialGoal` table
(additive migration) so it is a multi-day item, not a same-day one.

## Sandbox limitations (note for accuracy)

- `tsc --noEmit` and the **full** vitest suite **could not run here**: `prisma
  generate` is blocked in the sandbox (engine download returns 403), so the
  generated `@prisma/client` types are absent and every Prisma-dependent file
  fails to typecheck. This is an environment limit, **not** a project defect — the
  pure-function golden tests (which don't touch Prisma) all pass. **Run
  `npm run typecheck` and `npm run test` locally to confirm full green.**

## Suggested improvements implementable today

Ranked by value ÷ risk. The top pick is self-contained, needs **no migration**,
and reuses infrastructure already shipped for net-worth.

1. **FX currency-exposure report** *(recommended for today)*. Backlog item with
   strong Swiss fit: show how household assets/holdings split across currencies
   vs the CHF home currency, with concentration warnings. Reuses the existing
   `ExchangeRate` cache + the rate-map already built for `net-worth.ts`. New
   deterministic `src/lib/optimize/fx-exposure.ts` + golden tests + an Optimize
   tab and `/api/optimize/fx-exposure` route. **No schema migration. Low risk.**

2. **LICENSE + VERSION + CLAUDE.md** *(quick win, ~30 min)*. Root still lacks all
   three (plus `STYLING.md`). For a "polished, sellable" product a `LICENSE` is a
   real gap — pick a license deliberately (proprietary/EULA vs OSS, given the
   commercial intent). `VERSION` and `CLAUDE.md` are trivial hygiene.

3. **Inflation / real-terms toggle** *(half-day)*. Deterministic transform layered
   over existing `forecast.ts` / `projection.ts` outputs; no migration. Lets users
   read long-horizon numbers in today's francs — directly serves the
   "make finance legible to non-experts" mission.

4. **D2 financial goals / sinking funds** *(schedule, multi-day)*. The remaining
   Phase D feature. Needs a `FinancialGoal` migration, so plan it deliberately
   rather than starting same-day. Good candidate for the next dated task.

## Recommendation

Today: build **#1 FX currency-exposure report** (no migration, reuses net-worth
rate infra, Swiss-relevant) and land the **#2** repo-hygiene files alongside it.
Queue **D2 goals/sinking-funds** as the next dated milestone toward v0.9.0.
